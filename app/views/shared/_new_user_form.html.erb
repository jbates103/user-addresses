<%= form_for(@user)  do |f_user| %>
	<% if @user.errors.any? %>
		<div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  	<% end %>
	<table class="table table-striped">
		<tr>
			<td><%= f_user.label :first_name %>:</td>
			<td><%= f_user.text_field :first_name %></td>
		<tr>
		<tr>
			<td><%= f_user.label :last_name %>:</td>
			<td><%= f_user.text_field :last_name %></td>
		<tr>
		<tr>
			<td><%= f_user.label :social_security_number %>:</td>
			<td><%= f_user.text_field :ssi %></td>
		<tr>
	</table>
	<br>
	<%= f_user.submit class: "btn btn-primary" %>
<% end %>
<br>